{
  "name": "npa-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "storybook": "npm run storybook --workspace npa-ng-test",
    "build:ds": "npm run build --workspace npa-ng-test",
    "dev:demo": "npm run dev --workspace demo",
    "build:demo": "npm run build --workspace demo"
  }
}
